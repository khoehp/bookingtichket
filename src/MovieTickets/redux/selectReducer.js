const initialState = {
  selected: [],
};

const reducer = function (currnentState = initialState, action) {
  switch (action.type) {
    default:
      return currnentState;
  }
};

export default reducer;
